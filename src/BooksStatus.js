import React from 'react';
import { Button } from '@mui/material';

export default () => {
    const getBooksStatus = () => {

    }
    return (
        <>
            <Button style={{ float: 'right' }} variant="outlined" onClick={getBooksStatus}>Library Status</Button>
        </>
    )
}
